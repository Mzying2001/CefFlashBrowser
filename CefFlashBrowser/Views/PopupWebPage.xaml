<Window x:Class="CefFlashBrowser.Views.PopupWebPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CefFlashBrowser.Views"
        xmlns:flash="clr-namespace:CefFlashBrowser.FlashBrowser;assembly=CefFlashBrowser.FlashBrowser"
        mc:Ignorable="d"
        
        x:Name="window"
        Title="{Binding ElementName=browser, Path=Title, TargetNullValue=PopupWebPage}"
        Width="Auto"
        Height="Auto"
        MinWidth="400"
        MinHeight="300"
        Closing="WindowClosing">

    <Grid>
        <flash:ChromiumFlashBrowser x:Name="browser"/>
    </Grid>
</Window>
